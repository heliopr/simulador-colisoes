# Simulador de Colisões 3D

Primeiramente rode:
```
LINUX - setuplinux.sh
WINDOWS - setupwindows.bat
```

Em seguida, rode o arquivo encontrado em ./bin/Debug, ou se estiver no linux:
```
./run.sh
```
(adicione permissão de executável se necessário `chmod +x run.sh`)